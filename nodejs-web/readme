# Project Structure
nodejs-app/
    ├── index.js    # 서버 실행 코드
    ├── public/
    │   └── index.html  # 웹 페이지
    │   └── index.css   # 웹 페이지 디자인
    ├── result/     # 업로드한 이미지 처리 결과가 저장될 폴더
    │   └── json/       # 노화세포 개수, 노화세포 면적 비율, 실제 면적 등 정보가 저장되는 폴더
    │   └── segment/    # 검출된 노화세포의 이미지가 저장될 폴더
    ├── uploads/    # 업로드한 피부 세포 이미지가 저장될 폴더
    ├── sample/     # 예시 이미지 목록
    └── package.json

# Nodejs npm 실행 및 모듈 설치 명령어
$ npm init -y
$ npm install express multer axios child_process


